import {login} from "./actions";export default async function Page({searchParams}:{searchParams:Promise<{error?:string}>}){const p=await searchParams;return <main className="shell"><div className="card" style={{maxWidth:430,margin:"90px auto"}}><b className="brand">Mi Landing Web Fácil / Admin</b><h1>Ingresar</h1>{p.error&&<div className="error">{p.error}</div>}<form action={login} className="stack"><label className="label">Email<input name="email" type="email" required/></label><label className="label">Contraseña<input name="password" type="password" required/></label><button className="btn">Entrar</button></form></div></main>}