import {createClient} from "@/lib/supabase/server";import {notFound} from "next/navigation";import Link from "next/link";
export default async function QR({params}:{params:Promise<{id:string}>}){const {id}=await params;const s=await createClient();const {data:l}=await s.from("landings").select("*").eq("id",id).maybeSingle();if(!l)notFound();const url=(process.env.NEXT_PUBLIC_SITE_URL||"http://localhost:3000")+"/"+l.slug;const qr="https://api.qrserver.com/v1/create-qr-code/?size=700x700&data="+encodeURIComponent(url);return <main className="shell"><nav className="nav"><b>QR — {l.business_name}</b><Link className="btn secondary" href={"/admin/landings/"+id}>← Volver</Link></nav><div className="card" style={{maxWidth:620,margin:"30px auto",textAlign:"center"}}><h1>QR de la landing</h1><p className="muted">{url}</p><img src={qr} alt="QR" style={{width:"min(500px,100%)",display:"block",margin:"20px auto"}}/><a className="btn" href={qr} target="_blank">Abrir QR</a><p className="muted">Esta misma URL es la que grabás en la tag NFC.</p></div></main>}